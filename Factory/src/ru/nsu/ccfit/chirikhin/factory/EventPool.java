package ru.nsu.ccfit.chirikhin.factory;

import org.apache.log4j.Logger;
import ru.nsu.ccfit.chirikhin.blockingqueue.BlockingQueue;

/*public class EventPool<T> {
    private Logger logger = Logger.getLogger(EventPool.class.getName());
    int maxTaskCount;
    private BlockingQueue<T> events;
    Thread eventHandlerThread;

    public EventPool(int maxTaskCount) {
        this.maxTaskCount = maxTaskCount;
    }

    public T getEvent() {

    }
}
*/
