#include "ConsoleParser.h"

int main(int argc, char *argv[])
{
	ConsoleParser parser(argc, argv);

	//Editor(parser.getInputFilePath(), parser.getOutputFilePath());

	/*while(true)
	{
		try 
	}
	*/
	
	return 0;
}